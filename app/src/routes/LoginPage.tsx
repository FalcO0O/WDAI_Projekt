import React from "react";
import { Box } from "@mui/material";
import HomeBar from "../components/Common/HomeBar"; // Adjust the path as necessary

function LoginPage() {
  return (
    <Box>
      <HomeBar />
    </Box>
  );
}

export default LoginPage;
