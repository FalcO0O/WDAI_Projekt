{
  "name": "count-your-calories",
  "version": "1.0.0",
  "scripts": {
    "install-all2": "concurrently \"npm install --prefix app/backend\" \"npm install --prefix app/frontend\"",
    "install-all": "npm install --prefix ./ && npm install --prefix app/backend && npm install --prefix app/frontend",
    "start-all": "concurrently \"npm run dev --prefix app/backend\" \"npm run start --prefix app/frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "count-your-calories": "file:"
  }
}
